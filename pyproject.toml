[project]
name = "gemini-repl-009"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "files-to-prompt>=0.6",
    "llm>=0.26",
]
